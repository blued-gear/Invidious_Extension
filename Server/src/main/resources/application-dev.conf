{
  datasources {
    default {
      url = "jdbc:postgresql://localhost:5432/InvidiousExtension"
      username = "invidious_extension"
      password = ")|)4[*.y=5(=T5yx2V"
    }
  }

  jpa {
    default {
      properties {
        hibernate {
          show_sql = true
          format_sql = false
        }
      }
    }
  }

  micronaut {
    server {
      cors {
        localhost-pass-through: true
      }
    }
  }
}
