{
  datasources {
    default {
      url = "jdbc:postgresql://${INVIDIOUS_EXT_DB_HOST}/${INVIDIOUS_EXT_DB_NAME}"
      username = "${INVIDIOUS_EXT_DB_USER}"
      password = "${INVIDIOUS_EXT_DB_PASSWORD}"
    }
  }
}
