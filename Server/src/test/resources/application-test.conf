{
  datasources {
    default {
      db-type = h2
      driver-class-name = "org.h2.Driver"
      url = "jdbc:h2:mem:devDb;LOCK_TIMEOUT=10000;DB_CLOSE_ON_EXIT=FALSE"
      username = "sa"
      password = ""
    }
  }

  jpa {
    default {
      properties {
        hibernate {
          show_sql = true
          format_sql = true
        }
      }
    }
  }
}
